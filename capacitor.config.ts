import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brassbirmingham.solo',
  appName: 'Brass Móvil ES',
  webDir: 'dist'
};

export default config;
