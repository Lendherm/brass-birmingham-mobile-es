import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brassbirmingham.solo',
  appName: 'Brass Birmingham Solo',
  webDir: 'dist'
};

export default config;
